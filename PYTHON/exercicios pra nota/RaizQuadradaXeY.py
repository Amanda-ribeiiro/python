from math import pow

a = float(input("Digite o valor de a: "))
b = float(input("Digite o valor de b: "))
c = float(input("Digite o valor de c: "))

print(f"A equação é: ({a})*x^2 + ({b})*x + ({C})")

delta = b ** 2 - 4 * a * c


if delta < 0:
 print("esta equação não possui raízes reais")
else:
 raiz = (-b + pow(Delta, 1 / 2)) / 2 * a
 print("a raiz desta equação é", x = raiz) 
else:
  print("a raiz dupla desta equação é", x = {raiz})
  raiz1 = (-b + pow(Delta,1/2)) / 2 * a
  raiz2 = (-b - pow(Delta,1/2)) / 2 * a
  print("as raízes da equação são 1.0 e 2.0" x1 = (raiz1*2), x2 = (raiz2*2))
		